// Supabase helper (stub)
// This file contains stubs and instructions to integrate Supabase later.

// Install: npm install @supabase/supabase-js
// Usage: create a Supabase project, set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY in .env

/* Example:
import { createClient } from '@supabase/supabase-js'
const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY
export const supabase = createClient(supabaseUrl, supabaseAnonKey)

// Simple save function
export async function saveWishlist(userId, list){
  return supabase.from('wishlists').insert([{ user_id: userId, data: list }])
}
*/

export const note = 'Supabase integration stub — follow README to configure.'
